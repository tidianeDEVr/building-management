import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'building_man',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
